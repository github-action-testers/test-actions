name: Repository Events
on: [push]
jobs:
    checkout-test:
        runs-on: ubuntu-latest
        steps:
            - name: commit ids
              run: echo "$GITHUB_SHA"
            - name: branch name
              run: echo "$GITHUB_REF"
            - uses: actions/checkout@v4.1.1
